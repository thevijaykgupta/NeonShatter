/**
 * Enum representing the different states of the game
 */
public enum GameState {
    MENU,      // Initial menu/start screen
    PLAYING,   // Game is actively running
    GAME_OVER, // Player lost
    WIN        // Player won
}

